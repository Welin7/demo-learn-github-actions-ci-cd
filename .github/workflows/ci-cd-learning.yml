name: Hello World
on:
  push:
    branches:
      - master
      - development
      - 'release/**'  
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo build"